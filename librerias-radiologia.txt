conda activate medgemma_radio
python -m pip install --upgrade pip setuptools Wheel
pip install torch torchvision torchaudio

pip install transformers
pip install accelerate
pip install sentencepiece
pip install safetensors
pip install huggingface-hub
pip install tokenizers
pip install pillow
pip install numpy
pip install pandas
pip install matplotlib
pip install scikit-learn
pip install scipy
pip install tqdm
pip install requests
pip install python-dotenv
pip install google-genai
pip install soundfile
pip install librosa
pip install ipywidgets
pip install gradio
pip install opencv-python

pip install protobuf
pip install packaging
pip install psutil
pip install einops

pip install torch torchvision torchaudio transformers accelerate sentencepiece safetensors huggingface-hub tokenizers pillow numpy pandas matplotlib scikit-learn scipy tqdm requests python-dotenv google-genai soundfile librosa ipywidgets gradio opencv-python protobuf packaging psutil einops

Pero el código actual no utiliza directamente bitsandbytes, por lo que no es obligatorio. Si posteriormente quieres cuantizar MedGemma a 8-bit o 4-bit para reducir VRAM, entonces instala:
pip install bitsandbytes

pip install jupyter
pip install notebook
pip install ipykernel

python -m ipykernel install --user --name medgemma_radio --display-name "Python (MedGemma Radiologia)"